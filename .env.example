# Public site URL (used for canonical tags, sitemap, robots)
NEXT_PUBLIC_SITE_URL=https://example.com

# Optional: when both are set, contact form sends via Resend API.
RESEND_API_KEY=
CONTACT_TO_EMAIL=
